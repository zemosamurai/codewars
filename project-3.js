// split('') - чтобы получить массив из строки
// reverse() - чтобы перевернуть элемент в полученном массиве
// join('') - чтобы соединить элементы массива
function solution(str) {
   return str.split('').reverse().join('');
}
